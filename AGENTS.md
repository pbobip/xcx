## Agent skills

### Issue tracker

项目任务和 PRD 使用 GitHub 仓库 `pbobip/xcx` 的 Issues 管理；Pull Request 不作为需求入口。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单一上下文，在根目录使用 `CONTEXT.md`，架构决策存放在 `docs/adr/`。详见 `docs/agents/domain.md`。
